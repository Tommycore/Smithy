import * as vsc from 'vscode';

/** Registration of commands in the vs code environment. */
export function registerAssetCommands(context: vsc.ExtensionContext) {
  // TODO: Implement 'Get Asset' command.
}